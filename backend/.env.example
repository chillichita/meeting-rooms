# API port
PORT=8080

# Secret for signing JWT session cookies (used from MR-6)
JWT_SECRET=dev-secret-change-me

# SQLite database file path, relative to backend/ (used from MR-4)
DB_PATH=./data/meeting-rooms.db
